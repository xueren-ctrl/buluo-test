# ──────────────────────────────────────────────────────────
# Android 签名配置模板
# ──────────────────────────────────────────────────────────
# 用法：
#   1. 复制本文件为 keystore.properties（同目录，android/keystore.properties）
#   2. 生成 keystore（见下方命令），把生成的 .keystore 路径填到 storeFile
#   3. 填好四个字段后即可 ./gradlew assembleRelease 产出已签名 APK
#
# keystore.properties 已被 .gitignore 忽略，密钥不会进仓库。
#
# 生成 keystore 命令（用 Android Studio 自带 JDK 的 keytool，路径形如
#   "C:\Program Files\Android\Android Studio\jbr\bin\keytool.exe"）：
#
#   keytool -genkey -v -keystore buluo.keystore -alias buluo \
#     -keyalg RSA -keysize 2048 -validity 36500 \
#     -storepass 你的store密码 -keypass 你的key密码
#
# 提示会问姓名/单位等，可随意填（或用 -dname 参数跳过）。
# buluo.keystore 务必备份到安全位置，丢失后无法给同包名 APK 升级签名。
# ──────────────────────────────────────────────────────────

storeFile=buluo.keystore
storePassword=你的store密码
keyAlias=buluo
keyPassword=你的key密码
