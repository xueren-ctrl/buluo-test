# 2026-06-26

- 继续完成 CoC 升级提醒前端 PWA 改造收尾：核对 manifest、Service Worker、PWA 图标、离线页、样式、类型、API 服务文件。
- 修复浏览器通知权限判断错误，将 Notification.requestPermission 返回值判断从 `grant` 改为 `granted`。
- 重写 `hooks/usePwaInstall.ts`，避免 `deferredPrompt` 依赖导致重复注册监听，并加入 `appinstalled` 状态同步。
- 修复 IndexedDB 升级记录保存逻辑，去掉固定 `id: 0`，让 autoIncrement 正常生成主键，避免升级记录覆盖。
- 修复 Next.js 生产构建问题：UploadResponse 补充 `last_upload_at` 类型、通知状态检测改为 SSR 安全默认值、`/panel` 的 `useSearchParams` 增加 Suspense 边界。
- 使用 `npm --prefix frontend run build` 验证通过，首页 `/` 和 `/panel` 均成功静态预渲染。
- 根据用户要求移除 WxPusher 使用路径：前端不再要求填写 WxPusher UID，改为自动生成本地设备 ID 并使用浏览器通知/PWA 本地提醒；更新首页、教程、manifest 和 metadata 文案；构建验证通过。
- 升级 PWA 完整方案：安装 `next-pwa` + `workbox-window`；配置 `next.config.js` 实现 CacheFirst/NetworkFirst 缓存策略；创建完整 CoC 中文资源映射 `lib/coc-assets.ts`（含建筑/法术/英雄/宠物/装备/兵种/攻城机器/陷阱共 40+ 条目）；创建 `public/_headers` 确保 Cloudflare Pages 正确缓存；编写完整部署教程 `CLOUDFLARE_DEPLOY.md`；创建 `wrangler.toml` 配置；更新 `.gitignore`、`README.md`。
- 代码推送：修改 git remote 为 `https://github.com/xueren-ctrl/buluo.git`，724 文件（含前端 70+ 源文件 + 后端完整代码 + 部署文档）已提交并推送到 GitHub 新仓库。分支：main。
